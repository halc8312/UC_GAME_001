{
  "name": "uc-game-001",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Operation Undercurrent \u2014 a browser FPS vertical slice built on Three.js",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "verify": "npm run test && npm run build && npm run test:e2e",
    "capture": "node tools/capture.mjs",
    "profile": "node tools/profile.mjs",
    "rubric": "node tools/verify-rubric.mjs",
    "record:build": "node tools/record-build.mjs",
    "evidence": "npm run record:build && npm run test && npm run test:e2e && npm run capture && npm run profile && npm run rubric"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
